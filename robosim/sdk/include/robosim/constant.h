#ifndef CONSTANT_H__
#define CONSTANT_H__

/*
#if defined(WIN32)
    #define DIRECTORY_SEPERATOR     '\\'
    #define DLL_SELECTOR            "*.dll"
#elif defined(LINUX)
    #define DIRECTORY_SEPERATOR     '/'
    #define DLL_SELECTOR            "*.so"
#endif

#define DS  DIRECTORY_SEPERATOR
*/

/*
static const float EARTH_GRAVITY = 9.81f;    //地球重力加速度

static const float GLOBAL_CFM = 1e-5f;       //全局CFM
static const float GLOBAL_ERP = 0.2f;        //全局ERP

static const float DEFAULT_BOUNCE = 0.8f;        //默认弹性参数
static const float DEFALUT_BOUNCE_VEL = 0.1f;    //默认弹性最小速度

*/

#endif
